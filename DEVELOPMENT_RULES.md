Never change layout.

Never rename files.

Reuse components.

Never duplicate API.

Search project first.

Always use shadcn.

Business logic stays inside controllers.

Use async/await.

Use JWT.

Use bcrypt.

Use Sequelize.

Never use raw SQL.

Never use JSON datatype.
